# Copy this file to functions/.env for local Functions configuration.
# Keep production values in the deployment environment; do not commit secrets.
APP_ENV=development
DEV_ADMIN_ENABLED=true
FUNCTIONS_REGION=asia-east1

# Leave blank for the Firebase Emulator Suite. Set per environment when using a real project.
FIREBASE_PROJECT_ID=
